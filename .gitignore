# Personal / job-application materials — never commit
_personal/

# Python (the resume build tool)
__pycache__/
*.pyc

# Dependencies
node_modules/

# Wrangler / Cloudflare
.wrangler/
.dev.vars
.dev.vars.*

# Build output
dist/

# Env & secrets. The dotted forms aren't enough: a bare `env.live` or
# `env.prod` is not hidden by `.env.*`, and this repo is public.
.env
.env.*
env
env.*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
